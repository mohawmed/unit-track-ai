import React from 'react';
import { MOCK_TEAMS, PROGRESS_HISTORY } from '../../data/mockData';
import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer, RadarChart, Radar, PolarGrid, PolarAngleAxis, ResponsiveContainer as RC2 } from 'recharts';
import { TrendingUp, Users, CheckCircle2, AlertTriangle } from 'lucide-react';

// Reuse StudentChat for professor  
export { default } from '../student/StudentChat';
